using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BuyItem : MonoBehaviour
{
    /*GameObject buyMenu;
    IronCounter ironCounter;
    InventoryManager inventoryManager;
    // Start is called before the first frame update
    void Start()
    {
        buyMenu = GameObject.Find("BuyMenu");
        GameObject text = GameObject.Find("Text");
        ironCounter = text.GetComponent<IronCounter>();
        GameObject hotbar = GameObject.Find("Hotbar");
        inventoryManager = hotbar.GetComponent<InventoryManager>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void ClickedYes() {
        if(ironCounter.IronCount >= 10) {
            ironCounter.IronCount -= 10;
            inventoryManager.drillAvailable = true;
            inventoryManager.itemSelected = InventoryManager.Item.DRILL;
        }
            buyMenu.SetActive(false);
    }

    public void ClickedNo() {
        buyMenu.SetActive(false);
    }*/
}
